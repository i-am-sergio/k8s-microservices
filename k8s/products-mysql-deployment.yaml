apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: products-mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-mysql 
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-mysql
  template:
    metadata:
      labels:
        app: products-mysql 
    spec:
      containers:
        - name: products-mysql 
          image: mysql:8
          env:
            - name: MYSQL_ROOT_PASSWORD
              value: "root"
            - name: MYSQL_DATABASE
              value: "shopdb"
          ports:
            - containerPort: 3306
          volumeMounts:
            - mountPath: /var/lib/mysql
              name: products-mysql-storage
      volumes:
        - name: products-mysql-storage
          persistentVolumeClaim:
            claimName: mysql-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: products-mysql
spec:
  selector:
    app: products-mysql 
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306